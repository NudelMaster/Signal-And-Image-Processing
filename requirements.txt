# Signal & Image Processing — Project 1 dependencies
# Install with:  pip install -r requirements.txt
numpy==2.4.6
Pillow==12.2.0
matplotlib==3.10.9
tqdm==4.67.3
bm3d==4.0.3
torch==2.12.0        # Q3 only: median-inpainting initialisation
